import React, { useState } from 'react';

const Management = (): React.JSX.Element => {

    return (
        <div>
           <h1>Management</h1> 
        </div>
    );
};

export default Management;